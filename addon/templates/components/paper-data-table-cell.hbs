{{yield (hash
  parent=elementId
  toggleEdit=(action 'toggleEdit')
  showEdit=showEdit
  edit-dialog=(component 'paper-data-table-edit-dialog'
    parent=elementId
    onClose=(action 'close')
  ))
}}
